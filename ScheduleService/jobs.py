def send_message_remind(user_id, message):
    print(f'user {user_id} received "{message}"')  # tmp


def send_message_notif(user_id, message):
    print(f'user {user_id} received "{message} at the day"')  # tmp
